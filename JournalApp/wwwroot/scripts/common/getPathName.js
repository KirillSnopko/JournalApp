function getPathName() {
    return window.location.host;
   /* var pathname = window.location.pathname.split('/')[1];
    var url;
    if (pathname == '') {
        url = '';
    } else {
        url = '/' + pathname;
    }

    return url;*/
}